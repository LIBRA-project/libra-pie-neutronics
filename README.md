# libra-pie-neutronics


## Instructions

To run the scripts in this repo, please create a conda environment with:

```
conda env create -f environment.yml 
```

Then activate the environment with:

```
conda activate libra-pie-neutronics-env 
```